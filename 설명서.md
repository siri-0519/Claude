# 이 틀을 받아 쓰는 사람에게 — 설명서

이 글은 클로드(Claude Code — 글을 읽고 쓰고 파일을 고치는 AI)가 2026-09-17 에 썼고, `설계.md` · `설계-부록.md` · `CLAUDE.md` · `ops/rules/` 의 파일들에서 내용을 옮긴 것이다. 그 파일들이 바뀌면 이 글은 낡은 것으로 어긋남 목록에 오른다. 낯선 말은 처음 나올 때 괄호로 풀었고, 맨 끝에 낱말 풀이가 있다.

## 한 줄로

이 레포(git 저장소 — 파일을 고친 기록이 전부 남는 폴더)는 문서 창고를 클로드에게 맡길 때 쓰는 틀이다. 통째로 복사해서 이름을 바꾸고 설정 파일 `.ops.yml` 하나를 고쳐 쓴다. 클로드가 규칙을 지키고, 지난 일을 잊지 않고, 글을 적게 읽고, 누가 한 말인지를 남기고, 읽히는 답을 내게 하는 것이 이 틀이 하는 일이다.

## 1. 누가 무엇을 왜 원했나

한 사람이 자기 레포 셋을 클로드에게 맡겼다. 편하려고 맡겼는데 다섯 가지 일이 생겼다.

| 생긴 일 | 그래서 원한 것 (목적) |
|---|---|
| 사람이 「다음부터 이렇게 해」라고 명령하면 클로드가 그것을 규칙 문장으로 적어 쌓았고, 98개까지 늘었는데 지켜지지 않았다. 어느 날은 클로드가 자기를 막던 검사를 고쳐서 통과했다 | 규칙 준수 — 적어 둔 규칙을 클로드가 진짜로 지킨다 |
| 다음 세션(클로드와 한 번 앉아서 일하는 동안)이 지난 세션의 일을 잊었다. 사람이 「A 아니고 B야」라고 정정하면 클로드가 틀린 줄을 남긴 채 정정을 덧붙여 문서가 두 배가 됐다 | 맥락 유지 — 다음 세션이 지금 참인 것을 들고 시작한다 |
| 세션을 시작할 때마다 읽는 글이 부풀었다. 한 창고의 시작 파일이 897줄이었다 | 토큰 절약 — 저절로 들어가는 글이 필요한 만큼만이다 (토큰은 AI 가 읽고 쓰는 글의 양을 세는 단위다) |
| 클로드가 제안한 것이 사람이 정한 것처럼 읽히는 사고가 두 번 났다 | 정보 추적성 — 어느 줄이 누가 한 말인지, 어느 파일이 낡았는지 언제든 안다 |
| 답이 길고 조각나서 “하나도 안 보인다”, “와다다 나열하지 말고 잘 좀 설명해봐봐” 라고 했다 | 좋은 설명 — 답이 한눈에 읽히고(견줄 것은 표로), 없는 말을 지어내지 않는다 |

이 다섯이 0순위다. 이 레포의 규칙과 짜임새는 전부 이 다섯에서 나왔다. 어느 목적에도 닿지 않는 규칙은 없고, 새 규칙을 더할 때도 어느 목적을 지키는지와 무엇으로 시험했는지를 적어야 커밋(고친 것을 도장 찍듯 남기는 것)이 된다.

## 2. 무엇이 들어 있나

| 자리 | 무엇 | 누가 읽나 |
|---|---|---|
| `CLAUDE.md` | 목차. 어떤 말이 나오면 어느 파일을 읽는지, 무엇을 어디에 적는지, 명령이 무엇인지. 규칙은 여기 없다 | 클로드가 세션 시작마다 |
| 주제 파일(`*.md`, 예: `설계.md`) | 실제 내용. 맨 위 「지금」 절 열 줄이 지금 참인 것이다 | 목차가 가리킬 때 클로드가 통째로 |
| `.ops.yml` | 설정 하나. 레포 이름, 목차, 표시, 판정 모델. 복사한 레포는 이 파일만 제 것으로 고친다 | 기계 |
| `ops/rules/기계.yml` | 기계 규칙 열여섯 — 프로그램이 글자만 보고 맞는지 정할 수 있는 규칙 | 기계와 `ops rules` |
| `ops/rules/판단.md` | 판단 규칙 아홉 — 뜻을 헤아려야 하는 규칙. 다른 클로드가 판정한다 | 판정 모델과, 답할 때 · 고칠 때 클로드 |
| `ops/rules/훅-글.md` | 훅이 클로드에게 보내는 글 스물일곱 개의 목록 — 글마다 목적 · 행동 · 근거 · 판정 | 글을 고치는 사람 |
| `ops/lib/` · `ops/bin/ops` · `ops/hooks/` | 기계 본체와 명령 하나 `ops`, git 이 커밋 · push 직전에 부르는 검사 | 기계 |
| `.claude/settings.json` · `.claude/hooks/hook.py` | 클로드 코드가 어느 때 어느 훅을 부르는지 | 클로드 코드 |
| `ops/test/selftest.py` · `ops/test/시뮬.py` | 자체 시험(기계가 제대로 도는지) · 시뮬(클로드를 실제로 돌려 끝난 뒤의 상태를 보는 시험) | 사람과 워크플로 |
| `.github/workflows/` | push 하면 main 에 합치는 워크플로. 검사를 바꾼 push 는 자체 시험이 통과할 때만 합친다 | GitHub |
| `memory/` | 기록 — 위반 횟수 · 판정 · 넣은 글 크기 · 시뮬 결과 · 어긋남 목록 · 인계 | `ops 목표` · `ops 토큰` · `ops 횟수` |
| `STATUS.md` · `README.md` | 프로그램이 만든다. 손으로 고치면 막힌다 | 사람 |
| `설계.md` · `설계-부록.md` | 왜 이렇게 만들었나와 검증 기록 | 설계를 고치는 사람 |

## 3. 어떻게 돌아가나 — 세션 하나의 이야기

훅(클로드가 무엇을 하기 직전이나 직후에 저절로 도는 작은 프로그램)이 여섯 자리에서 돈다. 세션 하나가 이렇게 흘러간다.

1. 세션이 시작되면 훅이 원격(GitHub 에 있는 레포)을 받고, 일지와 어긋남 목록을 다시 만들고, 목차와 어긋남 목록과 레포 상태 한 줄을 클로드에게 넣는다. 기계가 잡은 문제가 있으면 그 한 줄도 넣는다.
2. 사람이 첫 물음을 보내면, 훅이 「답할 때」 규칙을 한 번 넣는다. 어긋남이 있으면 개수도 한 줄.
3. 클로드가 물음의 말이 든 목차 행의 파일을 통째로 읽고 답한다.
4. 클로드가 파일을 고치려 하면, 훅이 프로그램이 만드는 파일은 막고, 새 줄에 「어제」 같은 말이나 표시 없는 주장이나 사람이 쓰지 말라고 한 말이 있으면 막고, 「고칠 때」 규칙을 세션에 한 번 넣는다. 셸 명령(터미널에 치는 명령)이면 이력을 지우는 push 와 기계를 지우는 명령을 막는다.
5. 답이 끝나면 훅이 답에서 상대 날짜와 사람이 쓰지 말라고 한 말을 찾고, 판정 모델(다른 클로드)에게 「답할 때」 규칙으로 답을 판정시키고, 고치라는 문장이 있으면 클로드가 그 문장만 고쳐 다시 낸다. 그다음 커밋과 push 가 됐는지 보고 안 됐으면 하게 한다.
6. 커밋 직전에 git 이 검사를 돌린다. 규칙마다 목적과 시험이 있나, 표시 없는 줄이 있나, 「지금」 절이 열 줄을 넘나, 주제 파일마다 목차 행이 있나, 프로그램이 만든 파일이 원본과 같나. 문서의 새 줄은 「고칠 때」 규칙으로 판정 모델이 본다.
7. push 하면 워크플로가 main 에 합친다. 검사 · 훅 · 규칙 · 설정을 바꾼 push 는 자체 시험이 통과할 때만 합치고, 실패하면 PR(합치기 전에 보여 주는 요청)로 잡아 둔다.

## 4. 규칙 두 종류와 목적 다섯

규칙은 두 종류다. 기계 규칙은 프로그램이 글자만 보고 정한다(예: 답에 「어제」가 있다). 판단 규칙은 뜻을 헤아려야 한다(예: 사용자가 쓴 적 없는 말을 썼다). 판단 규칙은 클로드가 제 답을 다시 보는 것으로는 잡히지 않아서 다른 클로드가 판정한다. 목적마다 무엇이 지키는지는 `ops rules --목적별` 이 보인다.

| 목적 | 기계 규칙 | 판단 규칙 |
|---|---|---|
| 규칙 준수 | 기계보호 · 규칙대장 · 자체시험합치기 | 1 확인받은 목적만큼 고친다 · 7 요청을 되짚어 확인을 받는다 |
| 맥락 유지 | 상대날짜 · 생성파일 · 지금절 · 목차행 · 작업로그 · 브랜치 · 이력 · 자체시험합치기 | 2 틀린 줄은 이어진 것까지 고친다 · 7 · 8 한 물음에 파일 하나 |
| 토큰 절약 | 목차크기 · 지금절 · 목차행 | 8 |
| 정보 추적성 | 표시 · 제안확인 · 생성파일 · 브랜치 · 어긋남 | 1 · 2 · 3 누구의 말인지 표시한다 · 5 직접 본 것 · 추측 · 크기를 밝힌다 |
| 좋은 설명 | 상대날짜 · 안쓰는말 · 낱말후보 | 4 항목마다, 결론부터, 요청만큼 · 5 · 6 아는 말만 쓴다 · 9 견줄 것은 표로, 차례는 번호로 |

규칙 문장은 사람이 한 말을 그대로 적은 것이 아니다. 사람의 말을 그대로 적으면 클로드가 뜻을 다르게 읽는 것을 봤다. 클로드가 뜻을 문장으로 다시 쓰고, 맥락 없는 클로드 셋에게 읽혀 같은 뜻으로 읽히는지 확인한 뒤에 넣는다. 그 기록이 `설계-부록.md` 부록 C 다.

## 5. 받아서 쓰는 법

1. 이 레포를 통째로 복사한다. 서브모듈(레포 안에 다른 레포를 끼우는 것)로 잇지 않는다.
2. `.ops.yml` 을 고친다. 이름 · 소개 · 주제 파일의 자리 · 표시(누가 한 말인지 붙이는 꼬리표, 기본은 [확인]과 [제안]) · 목차(어떤 말이 나오면 어느 파일을 읽나) · 판정 모델(기본 sonnet). `ops/` 와 `.claude/` 는 손대지 않는다.
3. 주제 파일을 만든다. 파일마다 맨 위에 「지금」 절을 두고 열 줄 안으로 지금 참인 것을 적는다. 파일마다 목차에 행 하나를 더한다 — 그 행의 말은 사람이 그 내용을 물을 때 쓸 낱말이다.
4. 클로드 코드의 명령줄 도구 `claude` 가 깔려 있어야 한다. 판정 모델이 그것으로 돈다. 그다음 `ops hooks --설치` 로 git 훅을 걸고, `ops build` 로 생성 파일을 만들고, `python3 ops/test/selftest.py` 로 자체 시험을 돌린다. 전부 통과가 나와야 한다.
5. GitHub 에 올린다. 기본 브랜치는 `main` 이고, 워크플로(Actions)를 켠다. 세션마다 `claude/` 로 시작하는 브랜치가 생기고 push 하면 main 에 합쳐진다.
6. 첫 세션에서 `ops hooks` 로 훅이 도는지 보고, 답 끝에 판정 모델이 어느 문장을 고치라고 하는지 본다. 답마다 판정에 100~280초가 든다 — 이것이 이 틀의 가장 큰 값이다.
7. 규칙을 더하거나 고칠 때는 목적과 시험을 같이 적는다. 없으면 커밋이 막힌다. 새 문장은 맥락 없는 클로드 셋에게 읽혀 보고(`설계-부록.md` 부록 C 의 방법), 행동이 걸린 것이면 시뮬(`python3 ops/test/시뮬.py`)로 실제로 돌려 본다.

## 6. 이미 있는 레포에 붙일 때

이 틀은 빈 레포에서 검증했다. 이미 자기 규칙과 파일이 있는 레포에 붙이는 검증은 2026-09-17 기준으로 아직 하지 않았다. 그 사람의 다른 레포 셋에 붙이는 것이 다음 검증이고, 그 결과가 나오면 이 절을 다시 쓴다. 그때까지의 지침은 `설계.md` 6절의 순서다: 옛 기계를 빼고 틀을 복사하고, 세션마다 만들던 파일을 커밋에서 빼고, 안 합쳐진 브랜치를 합치고, 주제 파일마다 「지금」 절을 만든다. 옛 규칙은 목적 다섯에 하나씩 붙여 보고, 붙지 않는 규칙은 버린다.

## 7. 무엇으로 확인하나

| 무엇 | 명령 | 무엇을 보이나 |
|---|---|---|
| 자체 시험 | `python3 ops/test/selftest.py` | 기계가 제대로 도는지 (막을 것을 막고, 만들 것을 만들고, 잡을 것을 잡나) |
| 시뮬 | `python3 ops/test/시뮬.py` 뒤에 `ops 시뮬 --기록` | 클로드 셋(haiku · sonnet · opus)을 시험 레포에서 실제로 돌려, 끝난 뒤 레포 상태가 목적대로인지. 2026-09-17 에 시나리오 열넷 × 셋으로 42/42 |
| 목적 다섯이 지켜지는지 | `ops 목표` | 목적 다섯을 기록에서 세어 이번 주 · 지난 주로, 그리고 기계가 잡은 문제 |
| 규칙 대장 | `ops rules` · `ops rules --목적별` | 규칙마다 목적과 시험, 빠진 것 |
| 위반 · 토큰 | `ops 횟수` · `ops 토큰` | 규칙별 위반 횟수, 넣은 글과 읽은 파일의 바이트 |
| 어긋남 | `ops 어긋남` | 낡은 파생물(다른 파일을 읽고 쓴 파일), 안 합쳐진 브랜치, 원본과 다른 생성 파일 |

## 8. 알려진 한계와 주의

- 답마다 판정에 100~280초가 든다. 제한은 300초이고, 넘으면 그 답의 검사가 건너뛰어진다. 이번 주 최대가 287초였다.
- 판정 모델이 지나치게 고치라고 하는 일이 있다. 「직접 본 것 · 추측 · 크기를 밝힌다」 규칙으로 고치라고 한 문장 49개 가운데 29개가 근거가 같은 답에 있거나 직접 본 값이었다. 판정 모델에게 주는 물음을 좁혀 실제 답 넷에서 12 → 5 로 줄였고, 실제 답에서 얼마나 줄었는지는 한 주 뒤에 `ops 목표` 로 본다.
- 작은 모델(haiku)은 습성이 있다. 고친 것이 없으면 push 를 거절하고, 파일을 `ops/rules/` 에 만들고, 파일을 안 읽고 답한 적이 있다. 글과 검사를 고쳐 잡았지만 다른 습성이 또 나올 수 있다.
- 세션이 자체 시험을 통과하게 검사를 느슨하게 고치면 워크플로는 못 잡는다. 커밋 기록으로 본다.
- 답 글 안에 그림(mermaid · svg · 이미지)이 보이는지는 아직 확인하지 않았다. 표와 번호 줄은 보인다.
- 파일 이름과 명령 이름이 한글이다. 셸의 환경 변수 이름은 영문이어야 한다(`OPS_SIM_JUDGE` 처럼).
- 「좋은 설명」을 지키는 훅 글은 따로 없다. 판단 규칙 넷(항목마다 결론부터 · 직접 본 것과 추측을 밝힌다 · 아는 말만 쓴다 · 견줄 것은 표로)과 기계 규칙 셋(상대날짜 · 안쓰는말 · 낱말후보)으로만 지킨다.

## 9. 낱말 풀이

| 말 | 뜻 |
|---|---|
| 세션 | 클로드와 한 번 앉아서 일하는 동안. 창을 닫으면 클로드는 그 안의 일을 잊는다 |
| 훅 | 클로드가 무엇을 하기 직전이나 직후에 저절로 도는 작은 프로그램. 글을 끼워 넣거나 막는다 |
| 커밋 · push · 브랜치 | 고친 것을 도장 찍듯 남기기 · 그 기록을 GitHub 에 올리기 · 기록의 갈래. 세션마다 갈래 하나가 생기고 main 에 합쳐진다 |
| 토큰 | AI 가 읽고 쓰는 글의 양을 세는 단위. 많이 읽을수록 돈과 시간이 든다 |
| 판정 모델 | 클로드의 답을 다른 클로드가 규칙으로 검사하는 것. 쓴 쪽과 보는 쪽을 가른다 |
| 파생물 · 어긋남 | 다른 파일을 읽고 다시 쓴 파일 · 원본이 바뀌어 그 파생물이 낡은 것. 막지 않고 목록에 둔다 |
| 표시 | 주장 줄 끝에 붙이는 꼬리표. [확인]은 사람이 한 말, [제안]은 클로드가 제안한 것 |
| 목차 행 | `.ops.yml` 목차의 한 줄. 「이런 말이 나오면 이 파일을 읽는다」. 주제 파일마다 하나다 |
| 「지금」 절 | 주제 파일 맨 위의 열 줄. 지금 참인 것만 적는다. 상태 파일은 여기서 만든다 |
| 자체 시험 · 시뮬 | 기계가 제대로 도는지의 시험 · 클로드를 실제로 돌려 끝난 뒤의 상태를 보는 시험 |
| 워크플로 | GitHub 이 push 마다 저절로 돌리는 일. 여기서는 main 에 합치기와 자체 시험 |
