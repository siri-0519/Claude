# 설계 — 틀 레포가 지켜야 하는 것

2026-09-13과 2026-09-14에 본인과 상의한 설계다. 표시는 둘이다. [확인]은 본인이 말한 것이고, [제안]은 내가 제안한 것이라 본인 확인 전이다.

## 지금

- 이 레포는 틀이다. 복사해서 이름을 바꿔 쓰고 서브모듈로 잇지 않는다 [확인 2026-09-14].
- 판단 규칙은 공용 여섯과 레포별 문장까지 확인됐다. 5절과 `설계-부록.md`의 부록 B가 전문이고 부록 C가 검증 기록이다 [확인 2026-09-14].
- 기본 브랜치를 main 으로 바꾸는 것은 본인이 GitHub 설정에서 한다 [확인 2026-09-14].
- 옮기는 순서 me → broadcast → creation 은 본인이 확인했다 [확인 2026-09-14].
- 6절 네 단계를 다 했다(1단계 2026-09-14: 기계는 `ops/`, 규칙은 `ops/rules/`, 설정은 `.ops.yml`). 4단계(creation)는 2026-09-15에 했다 — 서브모듈을 빼고, 답 끝 판정과 어긋남 목록만 틀에서 가져갔다. creation 은 자체 tools/ 를 그대로 두고, 틀에서 빌린 파일(판정 · 낱말 · 대화 · origin)만 틀이 바뀔 때 다시 복사한다 — 2026-09-17 에 에이전트가 이렇게 정했다. 2026-09-17 에 본 것: 빌린 판정.py 가 틀보다 33줄 뒤졌고, creation 의 답 끝 훅 제한 30초가 판정에 드는 시간(90초 넘음)보다 짧아 판정이 끝나기 전에 훅이 시간 제한으로 멈췄다 — 2026-09-17 에 고쳤다(제한 360초 · 판정.py 다시 복사 · 죽은 훅의 알림을 보이게 · 자체 시험 둘 · 합치기도 자체 시험으로). 레포 여럿 위에서 연 세션은 틀의 라우터가 네 레포의 훅을 돌린다 [제안 2026-09-17].
- 검사 · 훅 · 규칙 · 설정을 바꾼 push 는 워크플로가 main 에 합친 결과로 자체 시험(`ops/test/selftest.py`)을 돌려 통과하면 합치고, 실패하면 PR 로 잡아 두어 사용자가 GitHub 웹에서 정한다 — 2026-09-15 부터 그런 push 를 모두 PR 로 잡아 두고 사용자가 읽고 합치던 것(세션이 자기를 막는 검사를 고쳐서 통과한 2026-09-07 기록 때문)을, 사용자가 PR 을 읽지 않고 합치기만 하게 되어 2026-09-17 에 사용자가 이렇게 바꾸기를 승인했다 [확인 2026-09-17].
- creation 을 같은 기계로 바꿀지는 2026-09-17 에 사용자가 에이전트의 판단에 맡겼다 [확인 2026-09-17].
- 레포 여럿 위에서 연 세션의 답 끝 훅은 레포끼리 동시에 실행되고, 세션은 main 에 직접 push 하지 않는다 — push 직전 훅이 막는다 (2026-09-16). 검사 · 훅을 바꾼 push 가 자체 시험만 거쳐 main 에 들어가므로, 세션이 검사를 느슨하게 고친 것은 커밋 기록과 답의 알림으로 안다 (2026-09-17) [제안 2026-09-17].
- 판단 규칙에 일곱째 「요청을 되짚어 확인을 받는다」를 2026-09-16에, 여덟째 「한 물음에 파일 하나」(고칠 때)와 아홉째 「견줄 것은 표로, 차례는 번호로」(답할 때)를 2026-09-17에 더했고, 아홉째를 더하며 넷째에서 「요구하지 않은 표를 넣지 않는다」를 뺐다. 일곱째는 사용자가 보는 것은 에이전트가 이해한 의도가 맞는가 하나라는 사용자의 말에서, 여덟째는 파일을 역할별로 나누고 목차와 설명을 정확히 하라는 사용자의 2026-09-03 말에서, 아홉째는 답이 어려운 공통 원인이 "시각화의 부족함"이라는 사용자의 2026-09-17 말에서 왔다. 답 밖의 길(옆 창 그림 · 웹 페이지 · 문서)은 7절이 정한다. 셋 모델 사례 시험은 부록 C 에 있다 [제안 2026-09-17].
- 답 끝 판정 모델을 2026-09-16에 haiku 에서 sonnet 으로 바꿨다 — 시간은 비슷하고(평균 111초 대 120초) 규칙 7 사례를 12/12 잡아서다. 되돌림 횟수를 한 주 지켜본다 [제안 2026-09-16].

## 0. 이 레포는 틀이다 [확인]

이 레포를 복사하고 이름을 바꾸고, 그 사본을 그 레포의 목적에 맞게 고쳐 쓴다. 사본은 이 레포와 이어지지 않는다. 서브모듈로 붙이지 않고 핀도 없다. 지금 me · broadcast · creation에 붙어 있는 `.claude-ops` 서브모듈은 뺀다. 한 사본에서 고친 스크립트는 다른 사본으로 저절로 가지 않는다.

## 1. 목적 다섯과 재는 법 [제안]

목적은 다섯이고 0순위다. 규칙과 레포 구조는 전부 여기서 파생된 것이라, 실제로 돌려 봐서 목적을 못 이루는 구조는 갈아엎어도 된다. 사용자가 2026-09-17에 이렇게 정했고, 다섯째 「좋은 설명」은 「답변 시각화 + 없는 말 지어내지 않기」라고 했다 [확인 2026-09-17].
규칙마다 어느 목적을 지키는지와 무엇으로 시험했는지는 그 규칙 파일의 목적 · 시험 칸(`ops/rules/기계.yml` · `ops/rules/판단.md` 끝의 표 · `ops/rules/훅-글.md` 의 목적 · 판정 줄)에 있고, 빠지면 커밋이 막힌다(기계 규칙 「규칙대장」). `ops rules --목적별` 이 목적마다 무엇이 지키는지를 보이고, `ops 목표` 가 아래 재는 법을 기록에서 세어 보인다 [제안 2026-09-17].

| 목적 | 무엇이 되면 이룬 것인가 | 재는 법 [제안 2026-09-17] |
|---|---|---|
| 규칙 준수 | 판단이 드는 규칙까지 지켜진다 | 답 끝 판정을 시킨 번 가운데 걸린 번 · 판정 모델이 건 횟수와 기계가 막은 횟수와 본인이 고치라고 한 횟수를 규칙별로 센다 [제안 2026-09-17] |
| 맥락 유지 | 다음 세션이 지금 참인 것을 들고 시작하고, 틀린 사실로 답하지 않는다 | 본인이 "그건 바뀌었다"라고 고친 횟수(`ops 횟수 --본인 맥락`) · 세션 시작에 들어간 바이트 [제안 2026-09-17] |
| 토큰 절약 | 세션 시작과 답마다 자동으로 들어가는 글이 필요한 만큼만이다 | 시작과 답마다 들어간 바이트 · 막으며 보낸 글의 바이트 · Read 한 파일의 바이트 [제안 2026-09-17] |
| 정보 추적성 | 원본이 바뀌어 낡은 파생물이 전부 목록에 있고, 목록에 없는 낡은 파생물이 없으며, 주장 줄마다 누구의 말인지가 붙어 있다 | 목록에 없다가 본인이 찾아낸 낡은 파생물의 수(`ops 횟수 --본인 어긋남`) · 어긋남 목록의 항 수 · 표시 없는 줄을 막은 횟수 [제안 2026-09-17] |
| 좋은 설명 | 답이 견줄 것은 표로, 차례는 번호로 보이고, 문서에 없는 것은 없다고 말하고, 사용자와 문서의 말로만 적힌다 | 판정 모델이 규칙 4 · 5 · 6 · 9 로 건 횟수 · 상대 날짜로 막은 횟수 · 본인이 못 알아들었다고 한 횟수(`ops 횟수 --본인 설명`) [제안 2026-09-17] |

횟수는 `memory/횟수.jsonl`에 한 줄씩 쌓는다. 누가 잡았나(판정 모델 · 본인), 규칙 이름, 날짜만 적는다. 주마다 규칙별로 세어 보면 어느 규칙이 아직 안 지켜지는지 나온다.

## 2. 맥락 유지 [확인 — 방향]

사실은 한 자리에만 있다. 주제 파일 맨 위에 「지금」 절을 두고 열 줄 안으로 유지한다. 지금 참인 것은 그 절에만 적는다.

STATUS.md와 README.md와 CLAUDE.md 표의 "안 읽으면 틀리는 것" 칸은 「지금」 절에서 스크립트가 만든다. 손으로 고치면 훅이 막는다. 그래서 같은 사실이 두 값으로 갈릴 자리가 없다.

worklog.md는 커밋하지 않는다. 세션 시작 때 git log에서 만든다. 2026-09-13에 확인한 바로 me의 안 합쳐진 세션 브랜치 넷은 전부 worklog.md에서 충돌했고 셋은 거기서만 충돌했다. 세션마다 다시 만들어 커밋하는 파일은 세션 둘이 겹치면 반드시 충돌한다.

결정 로그(decisions.md)는 로그다. 주제 파일의 「지금」이 정본이고, D-nnn은 그 줄이 어느 결정에서 왔는지 가리키는 번호다. "정본이 둘"이라는 규칙은 없앤다.

세션 시작에 들어가는 것은 둘이다. 목차(CLAUDE.md)와 어긋남 목록(4절)이다. 「지금」 절은 넣지 않고, 물음이 그 주제에 닿을 때 그 파일을 읽는다.

## 3. 토큰 절약 [확인 — 방향]

규칙은 쓰이는 자리에만 들어간다. 문서를 고치는 규칙은 Edit · Write 직전에 들어간다. 답의 규칙은 넣지 않고 답이 끝날 때 판정한다(5절). 세션 시작에는 2절의 둘만 들어간다.

주제 파일 하나는 목차 행 하나다. 그 행의 말로 물을 것은 전부 그 파일에 있어서, 한 물음에 파일 하나를 통째로 읽으면 된다. 파일을 나누는 기준은 크기가 아니라 이것이고, 나눌지는 에이전트가 어느 절이 어느 일에 속하는지를 적어 물으면 사용자가 정한다 (판단 규칙 8 · 기계 규칙 「목차행」). 크기 대신 답마다 Read 한 파일과 바이트를 `memory/문맥.jsonl` 에 적고 `ops 토큰` 으로 본다 [제안 2026-09-17].

2026-09-14 부터 2026-09-17 까지 있던 「30KB 가 넘는 주제 파일은 절로 나눠서 한 번에 한 절을 읽는다」 는 에이전트가 정한 수였고, 사용자가 2026-09-03 에 creation 에서 말한 것(필요한 절만 읽는 것보다 파일을 역할별로 나누고 목차와 설명을 정확히 하는 것)과 어긋나 없앴다. 견준 기록은 `설계-부록.md` 부록 C 에 있다 [제안 2026-09-17].

문서 레포에는 글만 둔다. 매시간 받는 자료와 사진은 자료 레포나 git 밖에 둔다.

## 4. 파생 데이터 추적 — 막지 않고 목록에 둔다 [확인]

파생물은 다른 파일에서 내용이 온 파일이다. 두 종류다.

**첫째, 스크립트가 원본에서 통째로 만들 수 있는 파일.** worklog.md, rules.md, STATUS.md, 목차의 셋째 칸이 여기 든다. 이 파일은 손으로 고치지 않는다. 원본을 고치고 `ops build`를 돌린다. Edit로 고치려 하면 훅이 막는다. 원본과 다른 채로 남아 있으면 어긋남 목록에 오른다.

**둘째, 사람이나 내가 원본을 읽고 다시 쓰는 파일.** creation의 원고, broadcast의 트레이너 설명서가 여기 든다. 옆의 `<파일>.meta.yml`에 출처 파일마다 셋을 적는다. 파일 경로, 그때의 커밋, 그때의 내용 해시다.

**어긋남 목록**(`memory/어긋남.md`)은 스크립트가 만든다. 원본의 해시가 달라진 파생물마다 한 항이다. 항에는 파생물, 출처, 어긋나기 시작한 날, 바뀐 절의 제목이 있다. 바뀐 절은 `git diff <그때 커밋>..HEAD -- <출처>`에서 뽑는다. 원본이 한 세션에 여러 번 바뀌어도 항은 하나고 바뀐 절만 늘어난다.

목록은 막지 않는다. 답이 끝날 때 훅이 "어긋남 N개, 이번 세션에 새로 M개"만 한 줄로 알린다. 세션 시작 때 목록 전체가 문맥에 들어간다. 그래서 세션이 바뀌어도 잊히지 않는다. 목록 파일은 세션마다 다시 만들고 커밋하지 않는다. 커밋하면 worklog.md처럼 세션 둘이 겹칠 때 충돌한다. 어긋나기 시작한 날은 파일에 적어 두지 않고 git log에서 읽는다 [제안].

항을 지우는 길은 둘이다. 파생물을 고치고 `ops ack <파생물>`을 돌리면 해시가 다시 맞는다. 고칠 것이 없으면 `ops ack <파생물> --그대로 "<이유>"`를 돌리고 이유가 로그에 남는다.

같은 목록에 이것도 오른다. main에 안 합쳐진 세션 브랜치. 폐기된 결정 번호를 단 주제 파일의 줄. 원본과 다른 생성 파일.

## 5. 규칙 준수 — 규칙은 두 종류다 [확인 — 방향]

### 기계 규칙

스크립트가 글자만 보고 맞는지 정할 수 있는 규칙이다. 개수 제한이 없다. 지금 필요한 것은 이렇다.

- 답과 문서에 시점을 가리키는 상대 표현을 쓰지 않는다. 날짜를 적는다.
- 주제 파일에 새로 들어가는 주장 줄에는 [실측] · [확인] · [제안] · [미측정] 가운데 하나가 붙어 있다.
- [제안]이 [확인]으로 바뀌는 것은 본인이 GitHub 웹에서 한 커밋에서만 된다. 검사 전부터 있던 항은 전부 기존으로 두고 바뀌는 순간만 본다.
- CLAUDE.md는 800줄, 한 절은 120줄을 넘지 않는다.
- 스크립트가 만드는 파일은 손으로 고치지 않는다.
- 세션이 끝날 때 이 세션의 브랜치가 원격에 있다.
- 이력을 다시 쓰는 push를 하지 않는다.
- 답의 낱말 가운데 본인의 말 · 이 세션에 읽은 파일 · 허용 목록 어디에도 없는 낱말을 뽑아 판정 모델에 넘긴다. 사고 때마다 낱말을 목록에 더하는 방식은 그만둔다.

### 판단 규칙

글을 읽고 뜻을 헤아려야 맞는지 정할 수 있는 규칙이다. 틀에서는 아홉이다 [제안 2026-09-17].

답이 끝날 때 훅이 다른 모델을 부른다. `claude -p --model haiku`가 이 환경에서 도는 것을 2026-09-13에 확인했다. 훅은 넷을 넘긴다. 사용자의 이번 요청, 에이전트의 답, 아래 판단 규칙, 이 답에서 바뀐 파일 목록이다. 모델은 어긴 규칙과 어긴 문장만 돌려준다. 어긴 것이 있으면 훅이 답을 막고 그 문장을 에이전트에게 보여주고, 에이전트는 그 문장만 고친다. 커밋 직전에는 같은 판정을 문서의 바뀐 줄에 한다.

쓴 쪽과 보는 쪽이 다르기 때문에 된다. 에이전트가 자기 답을 다시 보는 것은 안 된다는 것이 2026-09-13 세션에서 드러났다. 물음 하나만 받은 다른 모델은 잡는다.

아래 문장이 훅이 넘기는 규칙 전문이다. 사용자와 에이전트를 이름으로 가르고, 이 저장소를 모르는 에이전트가 읽어도 같은 뜻으로 읽히게 썼다. 2026-09-14에 haiku · sonnet · opus 셋에게 규칙만 주고 자기 말로 다시 쓰게 하고 사례 25개를 판정하게 하는 것을 여섯 판 되풀이했다. 마지막 판에서 haiku · sonnet · opus 셋의 판정 25개가 전부 의도와 같았다. 같은 날 밤에 규칙 98개의 원래 의도를 기록에서 찾아 실제 사고로 사례를 만들고 세 판을 더 돌렸다. 그때 공용 2 · 3 · 4 · 6에 문장이 붙었다. 없는 것은 줄을 지운다, 표시는 저장소의 표시 가운데 맞는 것을 붙인다, 줄여서 뜻이 안 통하게 하지 않는다, 빗대는 말을 쓰지 않는다. 기록은 `설계-부록.md`의 부록 C에 있다. 남은 모호함은 문장으로 없앨 수 없는 것이다. 어디까지가 그 변경 때문에 어긋난 줄인가, 항목을 몇 개로 세나, 깊이를 무엇으로 재나는 판정 모델이 사례를 보고 정한다.

#### 판단 규칙 전문 [확인 2026-09-14]

용어. 사용자는 저장소 주인인 사람이다. 에이전트는 사용자의 요청을 받아 답하고 문서를 고치는 AI다. 문서는 저장소 안의 파일이다. 요청은 사용자가 이번에 보낸 말이고, 항목은 그 안에서 따로 답이나 일을 요구하는 물음이나 지시 하나다. 번호가 없어도 센다. 주장은 참인지 거짓인지 가릴 수 있는 문장이고, 판정은 좋다 · 나쁘다 · 같다 · 낫다처럼 대상을 평가하는 말이다. 표시는 주장이나 판정 앞에 붙이는 꼬리표다. 표시의 종류와, 뒤집힌 결정을 남겨 두는 문서가 무엇인지는 저장소 규칙 파일이 정한다. 예를 들면 [확인]은 사용자가 말한 것, [제안]은 에이전트가 제안한 것, [실측]은 측정한 값이다.

고칠 때

1. 시킨 것만 고친다. 요청이 요구하는 것만 고치거나 만든다. 그 변경 때문에 값이나 뜻이 어긋나게 되는 줄은 저장소 어디에 있든 같이 고친다. 전부터 틀려 있던 줄과 그 밖에 고칠 것은 고치지 않고 답에 한 줄로 알린다.
2. 틀린 줄은 바꿔 넣는다. 고치는 줄이 틀렸으면 지금 맞는 문장으로 바꿔 넣고, 틀린 줄을 남긴 채 아래에 정정을 덧붙이지 않는다. 사실을 적을 때는 "X가 아니다" 대신 "Y다"로 적는다. 있다고 적었는데 실제로는 없는 것이면 "없다"로 고치지 않고 그 줄을 지운다. 하지 않기로 한 결정은 사실이 아니라 결정이므로 그대로 적는다. 뒤집힌 결정을 남겨 두는 문서에서는 취소선으로 남긴다.
3. 누구의 말인지 표시한다. 문서에 새로 적는 주장과 판정마다 이 저장소의 표시 가운데 맞는 것을 붙인다. 사용자가 말한 것과 에이전트가 제안한 것은 늘 갈라 붙이고, 그 밖의 표시는 용어가 정한 대로 붙인다. 뜻이 바뀌게 고쳐 쓴 줄은 새로 적는 것이다. 뜻을 바꾸지 않고 옮겨 적을 때는 원래 표시를 그대로 둔다. 문서에서는 이 표시가 규칙 5의 구분을 맡는다.

답할 때

4. 항목마다, 결론부터, 요청만큼. 요청의 항목마다 답하고, 답 전체도 항목마다의 답도 결론부터 적는다. 답할 수 없는 항목에는 그 자리에 왜 못 하는지를 적는다. 분량은 항목 수와 사용자가 요구한 깊이에 맞추고, 요구하지 않은 배경 설명을 넣지 않는다. 줄여서 뜻이 안 통하게 하지도 않는다. 읽는 사람이 이해하는 데 필요한 만큼은 완전한 문장으로 풀어 쓴다. 규칙 1의 알림 한 줄은 요구하지 않았어도 넣는다. [제안 2026-09-17 — 「과 표」를 뺌]
5. 직접 본 것 · 추측 · 크기를 밝힌다. 이 세션에서 파일 · 기록 · 명령 결과를 직접 본 것에는 무엇을 봤는지를 붙이고, 그 밖의 것에는 추측이라고 붙인다. 문서에서 찾지 못한 것은 "문서에 없다"라고 말하고, 기억으로 아는 값을 문서의 값처럼 말하지 않는다. 문서에 [제안]으로 적힌 것을 정해진 것처럼 말하지 않는다. 판정에는 왜 그런지와 얼마나 그런지를 바로 붙여 적는다. 얼마나는 수치로, 어림이면 어림이라고, 없으면 확인하지 않았다고 적는다.
6. 아는 말만 쓴다. 사물 · 방법 · 절차를 부를 때 사용자가 쓴 적 있는 말이나 저장소 문서에 있는 말을 쓴다. 그 밖의 말은 세상에 이미 있는 용어라도 쓰지 않고, 그 자리에 그것이 무엇인지를 쓴다. 에이전트가 처음 붙이는 이름도 쓰지 않고 그 자리에 하는 일을 적는다. 다른 것에 빗대는 말도 쓰지 않고, 실제로 무엇이 어떻게 되는지를 적는다.
7. 요청을 되짚어 확인을 받는다. 다음 두 경우에는 일을 시작하기 전에 이해한 목적과 할 일의 범위를 한두 문장으로 적고 사용자의 승인을 받는다 — 요청을 두 가지 이상으로 읽을 수 있을 때, 그리고 요청에 무엇을 위해 하는지(목적)가 없는데 답이 파일을 새로 만들거나 문서에 절을 새로 적거나 검사를 붙일 때. 승인 전에는 파일을 고치지 않는다. 한 가지로만 읽히는 요청("1 고쳐" · "이 줄 지워")과 읽기만 하는 물음에는 되묻지 않고 바로 한다. 사용자가 이미 목적을 말하고 승인했으면 다시 묻지 않는다. [제안 2026-09-16]
9. 견줄 것은 표로, 차례는 번호로. 셋 이상을 같은 잣대 둘 이상으로 견주는 것 — 파일마다 무엇을 담나, 방법마다 무엇에 맞나, 레포마다 무엇이 어디까지 됐나, 모델마다 시험 결과가 어떤가 — 은 문장으로 늘어놓지 않고 표 하나로 낸다. 표 바로 앞에는 무엇을 견주는 표인지 한 문장을 적는다. 먼저 무엇이 되고 다음에 무엇이 되는 차례를 셋 이상, 단계마다 무엇을 하고 어떻게 되는지까지 설명할 때는 한 문단에 잇지 않고 번호를 붙인 줄로 하나씩 낸다. 단계의 이름만 한 문장으로 요약한 것은 문장으로 둔다. 표나 번호 줄로 낸 것을 같은 답에서 문장으로 다시 적지 않는다. 답 밖에 그림을 보냈으면 답에 그 그림이 무엇을 보이는지 한 문장을 적는다. [제안 2026-09-17]

#### 규칙마다 왜 있는가 (훅이 넘기지 않는 참고)

1. 사용자가 시키지 않은 변경은 사용자가 모르는 채로 정본에 들어가고, 다음 세션이 그것을 사실로 읽는다.
2. 틀린 줄을 남기고 덧붙이면 문서가 두 배가 되고 틀린 문장을 다음 세션이 읽는다. 2026-08-26까지 그 방식으로 분량이 두 배가 됐다.
3. 사용자가 정한 것과 에이전트가 제안한 것이 같은 문장으로 앉으면 다음 세션이 에이전트의 제안을 사용자의 결정으로 읽는다. 그 사고가 broadcast에서 두 번 났다.
4. 사용자가 읽는 시간이 비용이고, 긴 답에서는 결론이 묻히며, 빠진 항목은 사용자가 다시 물어야 한다. 2026-09-13에 "하나도 안 보인다"가 그 결과였다. 반대로 줄여서 뜻이 안 통하는 답도 사용자가 다시 물어야 한다. 사용자는 2026-09-14에 "줄여버리니까 오히려 이해가 안 될 때가 많음"이라고 했고, 조각 문장을 해석하는 데 큰 어려움이 있다.
5. 사실처럼 쓴 추측이 문서에 들어가면 다음 세션이 그것을 근거로 조언한다. 크기 없는 판정은 틀려도 틀린 줄 모른다. 2026-09-04에 "자면 덜 쓰니 안 먹은 것과 그게 그거"가 실제로는 100 대 234였다.
6. 에이전트가 붙인 이름은 문서 밖에 없어서 사용자가 물어야만 뜻을 알고, 세션이 바뀌면 에이전트도 모른다. 빗대는 말은 문서를 몇 번 건너가면 뜻이 사라진 채로 원고에 섞인다. 사용자가 2026-09-07에 "또 막 빚 이런 용어 쓴다"라고 했다.
7. 에이전트가 이해한 목표와 사용자의 목표가 다른 채로 구현이 진행되면, 그 결과가 사용자의 말이 아닌 에이전트의 말로 정본에 들어가고 사용자는 맞는지 볼 수도 없다 [제안 2026-09-16].
   사용자가 2026-09-16에 "내가 원했던 목표랑 너가 이해하고 이루고자 하는 목표가 다른 상태로 맹목적으로 구현만 했다" 고 짚었다 [확인 2026-09-16].
9. 세 가지를 문장으로 늘어놓으면 사용자가 문장을 다시 표로 머릿속에서 짜야 읽힌다 [제안 2026-09-17].
   사용자가 2026-09-17에 답이 어려운 공통 원인을 "시각화의 부족함"이라고 하고, 시각화를 잘 적용하면 답을 이해하기 쉬워질 것이라고 했다 [확인 2026-09-17].

### 사본마다 바꿔도 되는 것

아홉 가운데 그 레포에 안 맞는 것은 그 레포의 규칙으로 바꾸고, 그 레포에서만 거는 규칙을 더한다. 레포마다의 문장은 `설계-부록.md`의 부록 B에 있다. me는 넷을 더해 열셋, broadcast는 둘을 더해 열하나, creation은 답할 때 여덟과 문서를 고칠 때 열하나다. 열하나까지는 에이전트 셋이 사례를 의도대로 잡는 것을 확인했고, 실제 답을 매번 판정할 때의 놓침은 횟수 기록으로 본다. creation의 원고 규칙은 원고 파일을 고치기 직전에 들어가고, 커밋 직전 판정에는 지침을 같이 넘긴다 [제안 2026-09-17].

## 6. 옮기는 순서 [제안]

1. 이 레포를 틀로 다시 짠다. 이 설계, 규칙 대장 두 종류, 훅(답 끝 판정 · 어긋남 목록 · 낱말 거르기 · 표시 검사 · 생성 파일), main 브랜치. 기본 브랜치를 main으로 바꾸는 것은 본인이 GitHub 설정에서 한다.
2. me. `.claude-ops`를 빼고 틀에서 복사한다. worklog.md를 커밋에서 뺀다. 안 합쳐진 브랜치 넷을 합친다. 주제 파일마다 「지금」 절을 만들고 STATUS.md를 거기서 만든다. 사진을 git 밖으로 낸다 [제안 2026-09-17].
3. broadcast. me와 같은 것에 더해, 치지직 자료를 자료 레포로 보내고 README를 스크립트가 만들게 한다.
4. creation. `.claude-ops`를 빼고, 자체 tools/ops.py에 없는 것(답 끝 판정 · 어긋남 목록)만 틀에서 가져온다. 어긋나면 막던 것을 목록으로 바꾼다. STATE.md의 낡은 "막힌 것"을 지운다.

한 단계가 한 세션이고 레포마다 커밋 하나씩이다.

## 7. 답의 모양 — 무엇을 어느 모양으로 보이나 [제안 2026-09-17]

사용자가 2026-09-17에 답이 어려운 공통 원인을 "시각화의 부족함"이라고 하고, 시각화를 잘 적용하면 답을 이해하기 쉬워질 것이라며 착수하라고 했다 [확인 2026-09-17].
답 글 안의 모양(표 · 번호 줄 · 같은 것을 문장으로 되풀이하지 않기)은 규칙 9가 답 끝 판정에서 걸고, 답 밖의 길은 아래 표가 정한다 [제안 2026-09-17].
사용자는 세션 여럿을 한꺼번에 돌리며 답을 위에서 아래로 읽으므로, 답 글 안에서 보이는 모양을 먼저 쓰고 답 밖의 길은 답 글 안에서 안 되는 것에만 쓴다 [제안 2026-09-17].

| 답에 드는 것 | 모양 | 어디에 보이나 | 확인한 것 [제안 2026-09-17] |
|---|---|---|---|
| 셋 이상을 잣대 둘 이상으로 견줌 — 레포마다 무엇이 어디까지 됐나 · 모델마다 시험 결과 · 방법마다 무엇에 맞나 | 표. 바로 앞에 무엇을 견주는 표인지 한 문장 | 답 글 안 | 사용자가 2026-09-17에 표가 보인다고 했다 [확인 2026-09-17] |
| 차례 셋 이상 — 훅이 자리마다 하는 일 · 일을 하는 순서 | 번호 줄 | 답 글 안 | 번호 줄이 보이는 것은 2026-09-17의 답들로 봤다 [제안 2026-09-17] |
| 가지가 있는 흐름 — 어느 경우에 어디로 가나 | 그림(SVG 파일)을 대화 옆 창에 보내고, 답에는 그 그림이 무엇을 보이는지 한 문장 | 대화 옆 창 | 2026-09-17에 답 끝 훅의 흐름 그림(1,707 바이트)을 하나 보냈고, 사용자가 봤는지는 사용자가 알리면 안다 [제안 2026-09-17] |
| 긴 글 여럿을 나란히 견줌 — 되돌린 답 셋처럼 | 웹 페이지(아티팩트). 답에는 링크 한 줄 | 링크를 누르면 옆 창 | 2026-09-17 「되돌린 답 셋」 페이지(36,033 바이트)를 사용자가 보고 지시를 골랐다 [제안 2026-09-17] |
| 고쳐 가며 같이 보는 글 — 원고 첨삭 · 절차서 초안 | 문서(Claude Docs). 정본은 git 에 두고 문서는 파생물로 등록한다 | 링크 | 사용자가 2026-09-17에 "정본은 git에 두고, doc를 정말 하나의 도구처럼, 아예 검토/첨삭용 세션처럼 쓰는거네?" 라고 이해를 확인했다 [확인 2026-09-17] |
| 답 글 안의 그림 — mermaid · svg · 이미지 | 사용자가 시험 A · B · C 의 결과를 알리면 정한다 | 답 글 안 | 2026-09-17에 셋을 답에 넣어 보냈다 [제안 2026-09-17] |
| 고르는 물음(선택지 버튼) | 쓰지 않는다 | 화면 위 | 사용자가 2026-09-17에 화면을 가리고 강제하는 느낌이라 싫다고 했다 [확인 2026-09-17] |

답 밖의 길은 답에 드는 글보다 크다. 옆 창 그림 하나가 1,707 바이트, 웹 페이지 하나가 36,033 바이트였다(2026-09-17에 보낸 것의 크기). 그래서 표와 번호 줄로 되는 것은 답 글 안에서 끝내고, 그림은 가지가 있는 흐름에, 페이지는 긴 글 여럿의 견줌에만 쓴다 [제안 2026-09-17].
