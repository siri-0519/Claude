# 2026-09 작업 다이제스트

> GENERATED from `memory/log/2026-09.jsonl` by `ops/bin/ops build`. 하드 카피 금지 — 원본을 고치세요.

## 2026-09-04

- • **작업 환경 초기 구축** `work`
  규칙(hard/soft 2층) · 세션 문맥(STATE + jsonl 로그 + 자동 다이제스트) · 토큰(INDEX 지도 + 카드 상한) · 파생 연계(front-matter 해시 고정)를 훅으로 강제. selftest 48개 통과.
  ↳ `CLAUDE.md`, `ops/rules/hard.yml`, `ops/lib/core.py`
- ◆ **검사 가능한 규칙은 전부 훅으로, 나머지만 자연어로** `decision`
  CLAUDE.md의 규칙 표는 hard.yml/soft.md에서 생성 — 읽는 규칙과 집행하는 규칙이 어긋날 수 없음. 근거와 기각한 대안은 ADR-0001.
  ↳ `memory/decisions/ADR-0001-work-environment.md`
- · **훅을 직접 써보며 결함 2개 수정** `note`
  1) heredoc 본문을 명령어로 오인해 테스트 파일 작성 자체가 차단됨 -> strip_heredocs 도입. 2) 규칙 패턴을 서술하는 파일(테스트/규칙문서)에 예외 없음 -> content_scan_exempt 도입.
- → **다음 세션: soft.md를 실제 취향으로 교체하고 진짜 데이터 넣기** `handoff`
  ops/rules/soft.md의 SR-001~005는 시드값입니다. vault/는 아직 비어 있고 ADR 1건뿐.
- → **배선도 아티팩트 발행됨 · vault 편입 여부 미결** `handoff`
  세션 흐름/정보갱신 배선도를 아티팩트로 발행: https://claude.ai/code/artifact/406b41d4-c46f-468a-a9d1-1d06497a596d
  원본 HTML은 컨테이너 스크래치패드에만 있고 레포에는 없음 — 컨테이너가 회수되면 사라짐.
  미결: 이 배선도를 vault/docs/에 넣고 --source ops/rules/hard.yml 로 걸어둘지. 걸면 규칙 변경 시 자동으로 stale 잡힘.
  참고: 이 아티팩트는 wake 구독 등록 실패(403) 상태라 댓글/재발행이 세션을 깨우지 못함.
- · **SessionStart 훅은 아직 실제 세션에서 검증 안 됨** `note`
  구축 세션이 훅 등록보다 먼저 시작돼서 카드 주입을 실제로 관찰하지 못함. PreToolUse/UserPromptSubmit/Stop은 이 세션에서 실제로 동작 확인됨. 다음 세션 첫 화면에 STATE 카드가 뜨는지 확인할 것.
- • **작업 구조 검증 — 결함 3건 실증, 분류 체계 이중화 확인** `work`
  실행 검증: (1) ops new code vault/code/x.py 는 .py 에 --- YAML 펜스를 심어 SyntaxError 를 만듦 (is_texty 가 .py/.js/.ts/.sh 포함, join_frontmatter 는 확장자 무관 펜스). (2) HR-006 이 pre_tool 에서 vault/ 무메타 파일 생성을 차단해 vault/inbox 드롭이 불가 — ops ingest 의 존재 이유가 막힘. (3) escape: OPS_ALLOW 는 11개 중 HR-001/HR-002 에만 존재. (4) kind:doc 파일을 vault/records/ 에 두어도 ops check 통과 — 디렉터리와 kind 가 이중 진실원인데 정합성 검사가 없음. 반증된 것: id 접두사(X-/D-)는 new_id 에서 쓰기만 하고 되읽는 코드가 없어 장식임 — 승격 시 접두사 잔존은 기능 결함이 아니라 가독성 문제.
  ↳ `ops/rules/hard.yml`
- · **SessionStart 훅 실세션 검증 완료** `note`
  지난 세션이 미검증으로 남긴 항목. 이번 세션 첫 화면에 STATE 카드가 실제로 주입됐고 UserPromptSubmit 의 soft rules 재주입도 관찰됨. 훅 5종 중 SessionStart/UserPromptSubmit/PreToolUse 는 실세션 동작 확인. PostToolUse/Stop 은 이번 세션에서 미관찰.
- ◆ **콘텐츠 벡터: 자리만 확정, 활성화는 코퍼스 이후** `decision`
  라벨을 0/1 대신 벡터로 두자는 제안을 두 갈래로 갈라 판단. (1) 사람이 매기는 라벨 가중치는 기각 — 숫자의 출처가 재현되지 않고, 배타성 문제는 가중치 없는 다중 라벨로 이미 풀림. (2) 본문 파생 임베딩은 자리만 확정 — 저장 위치 .meta/vectors.jsonl (gitignore된 재생성 캐시), 레코드 [id, model, body_hash, vec], model 또는 body_hash 불일치 시 무효. front-matter에 넣지 않는 이유: derives_from 은 재계산 불가한 주장이라 파일과 함께 가야 하지만 벡터는 본문에서 재계산되는 캐시임. 기대 용도는 유사도 검색과 HR-009 stale 등급화(현재 pinned_hash != parent hash 이진이라 오타 수정이 재작성과 동일하게 턴을 막음). 지금 안 켜는 이유: vault 아티팩트 1건 edge 0건이라 임계값 보정 불가.
  ↳ `memory/decisions/ADR-0002-content-vectors.md`, `ops/schema/frontmatter.yml`
- • **검증에서 나온 결함 4건 수정 — 코드 파일 손상, inbox 잠김, 플래그 반복 유실, 문서 불일치** `work`
  ① is_texty 가 '텍스트냐'와 'front-matter 를 담을 수 있냐'를 겸하고 있어 .py/.js/.sh 에 --- 펜스가 박혔다. FRONTMATTER_SUFFIXES(.md/.txt)와 takes_frontmatter() 를 분리해 코드/바이너리는 .meta.yml 사이드카로 보낸다. cmd_new 는 사이드카 경로에서 --body 를 무시하고 touch 만 하던 것도 함께 고쳤다. ② HR-006 이 pre_tool 에서 vault/ 무메타 쓰기를 전부 막아 ops ingest 가 주울 대상이 생길 수 없었다. drop_zone()(= raw kind 의 home) 한 곳만 pre_tool 에서 열고, stop 단계 검사는 그대로 둬서 라벨 없이는 턴이 끝나지 않는다 — 보장은 유지하고 입구만 연다. ③ ADR-0001 이 OPS_ALLOW 를 일반 탈출구로 서술했으나 escape: 를 선언한 HR-001/HR-002 에만 듣는다. 해당 줄 정정, 그리고 escape 를 가지고도 안내가 없던 HR-002 message 에 사용법 추가. ④ --ref/--label/--source 가 action=append 없이 선언돼 플래그 반복 시 조용히 마지막 하나만 남았다. _csv 가 문자열/리스트를 모두 받게 하고 저장용 인자에 append 를 붙였다. 필터용 인자(ctx --ref, find --label)는 의미가 달라 건드리지 않았다. 검증: 실패했던 시나리오 4개를 그대로 재실행해 전부 통과, 48개 테스트 통과. 부수 확인: 처음으로 edge 2개를 만들어 stale 탐지를 실부하에서 확인 — 출처 수정 시 HR-009 차단, link ack 후 해제.
  ↳ `ops/lib/core.py`, `ops/bin/ops`, `ops/rules/hard.yml`, `memory/decisions/ADR-0001-work-environment.md`
- · **배선도 아티팩트 발행 · wake 구독은 두 세션 연속 403** `note`
  훅/규칙 배선 전체(경로·차단·예외·구멍·미결)를 아티팩트로 발행: https://claude.ai/code/artifact/2ffd34e6-9792-4792-8963-0a18d5f52102 . 원본 HTML은 컨테이너 스크래치패드에만 있고 레포에는 없음 — 컨테이너 회수 시 소실. wake 구독 등록 실패(403 subscribe_forbidden, 'subscribing requires a session credential'), 세션 종료까지 어떤 아티팩트도 구독 불가라 재시도 무의미. 지난 세션의 아티팩트(406b41d4)도 같은 403이었음 — 두 세션 연속이라 환경 제약으로 보이나 원인은 미확인. 실질 영향: 댓글/재발행이 세션을 깨우지 못함. 페이지 자체는 정상. 미결(지난 세션에서 이어짐): 이 배선도를 vault/docs/ 에 넣고 --source ops/rules/hard.yml 로 걸지 여부 — 걸면 규칙 변경 시 자동으로 stale 이 잡힘.
  ↳ `ops/rules/hard.yml`
- → **다음 세션: 배선도를 그리다 나온 결함 2건이 미수정으로 남음** `handoff`
  오늘 고친 4건과 성격이 같고 아직 안 고침. (A) HR-004(비밀정보)는 stage 가 pre_tool 뿐이라 결과 검사가 없다. HR-006/009/010 은 stop 에서 결과를 다시 보므로 인터프리터 쓰기(python3 -c open().write())로 우회해도 잡히지만, HR-004 는 어느 단계에서도 안 잡힌다 — pre_tool 의 _target_paths 가 리다이렉트/sed -i/편집도구 file_path 만 정규식으로 뽑기 때문. 이중 검사 설계에서 유일하게 빠진 자리. (B) evaluate() 의 fn = globals().get(rule['check']); if fn is None: continue — check 함수가 없으면 경고 없이 규칙이 통째로 건너뛰어진다. 크래시는 warn 을 내는데 부재는 침묵이라 비대칭. 함수명 오타 하나로 규칙이 소리 없이 사라질 수 있다. 둘 다 코드를 읽고 판단한 것이며 실제로 재현 시험은 하지 않았음.
  ↳ `ops/lib/core.py`, `ops/rules/hard.yml`
