# 2026-09 작업 다이제스트

> GENERATED from `memory/log/2026-09.jsonl` by `ops/bin/ops build`. 하드 카피 금지 — 원본을 고치세요.

## 2026-09-04

- • **작업 환경 초기 구축** `work`
  규칙(hard/soft 2층) · 세션 문맥(STATE + jsonl 로그 + 자동 다이제스트) · 토큰(INDEX 지도 + 카드 상한) · 파생 연계(front-matter 해시 고정)를 훅으로 강제. selftest 48개 통과.
  ↳ `CLAUDE.md`, `ops/rules/hard.yml`, `ops/lib/core.py`
- ◆ **검사 가능한 규칙은 전부 훅으로, 나머지만 자연어로** `decision`
  CLAUDE.md의 규칙 표는 hard.yml/soft.md에서 생성 — 읽는 규칙과 집행하는 규칙이 어긋날 수 없음. 근거와 기각한 대안은 ADR-0001.
  ↳ `memory/decisions/ADR-0001-work-environment.md`
- · **훅을 직접 써보며 결함 2개 수정** `note`
  1) heredoc 본문을 명령어로 오인해 테스트 파일 작성 자체가 차단됨 -> strip_heredocs 도입. 2) 규칙 패턴을 서술하는 파일(테스트/규칙문서)에 예외 없음 -> content_scan_exempt 도입.
- → **다음 세션: soft.md를 실제 취향으로 교체하고 진짜 데이터 넣기** `handoff`
  ops/rules/soft.md의 SR-001~005는 시드값입니다. vault/는 아직 비어 있고 ADR 1건뿐.
- → **배선도 아티팩트 발행됨 · vault 편입 여부 미결** `handoff`
  세션 흐름/정보갱신 배선도를 아티팩트로 발행: https://claude.ai/code/artifact/406b41d4-c46f-468a-a9d1-1d06497a596d
  원본 HTML은 컨테이너 스크래치패드에만 있고 레포에는 없음 — 컨테이너가 회수되면 사라짐.
  미결: 이 배선도를 vault/docs/에 넣고 --source ops/rules/hard.yml 로 걸어둘지. 걸면 규칙 변경 시 자동으로 stale 잡힘.
  참고: 이 아티팩트는 wake 구독 등록 실패(403) 상태라 댓글/재발행이 세션을 깨우지 못함.
- · **SessionStart 훅은 아직 실제 세션에서 검증 안 됨** `note`
  구축 세션이 훅 등록보다 먼저 시작돼서 카드 주입을 실제로 관찰하지 못함. PreToolUse/UserPromptSubmit/Stop은 이 세션에서 실제로 동작 확인됨. 다음 세션 첫 화면에 STATE 카드가 뜨는지 확인할 것.
- ◆ **누가 냈나 — 레포끼리 나눠 쓰는 기계를 여기에 두었다** `decision`
  이 계정의 레포 넷에 같은 병이 있다. 클로드가 적은 것과 사람이 정한 것이 같은 문장으로 앉아 있어서 다음 세션이 전부 사람의 결정으로 읽는다. broadcast 의 규칙 파일에 '제안이 서로 인용되며 확정으로 굳는 사고가 두 번 났다'고 적혀 있고, creation 에서는 떡밥 대장 42행 중 22행이 클로드가 넣은 것이었다. creation 에서 만들어 검증한 기계를 ops/lib/origin.py 로 뽑아 여기에 두고 다른 레포가 서브모듈로 붙여 쓴다. 복사하지 않는 이유는 복사본이 갈라지기 때문이다 — me 와 broadcast 의 규칙 파일이 같은 데서 복사됐는데 83줄 대 39줄로 갈라져 있다. 어느 문서를 세는가는 레포마다 다르므로 각 레포 뿌리의 .origin.yml 이 정한다. 판정은 사람이 직접 커밋한 줄에서만 받는다. 최종적으로는 플러그인으로 배포하고, 지금은 확실히 되는 서브모듈로 먼저 돌린다.
  ↳ `ops/lib/origin.py`
